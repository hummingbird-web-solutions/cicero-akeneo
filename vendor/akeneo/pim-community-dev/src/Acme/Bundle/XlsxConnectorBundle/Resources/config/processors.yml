parameters:
    acme.connector.processor.custom.class: Acme\Bundle\XlsxConnectorBundle\Connector\Processor\CustomXlsxProcessor

services:
    acme.connector.processor.custom_xlsx:
        class: '%acme.connector.processor.custom.class%'
        arguments:
            - '@pim_comment.builder.comment'
            - '@pim_comment.saver.comment'
            - '@pim_user.repository.user'